
# API Configuration
# آدرس پایه API بک‌اند (لاراول)

# API Configuration
# آدرس پایه API بک‌اند (لاراول)
VITE_API_BASE_URL=http://payesh.eitebar.ir/api

# ✅ آدرس پایه ذخیره‌سازی تصاویر و فایل‌های عمومی (Storage)
# این آدرس باید بدون / در انتها باشد.
VITE_STORAGE_BASE_URL=http://payesh.eitebar.ir/storage
# -----------------------------------------------------------------------------
# WebSocket (Pusher / Reverb) Configuration
# تنظیمات سوکت برای اتصال به Laravel Reverb یا Soketi
# -----------------------------------------------------------------------------

# کلید اپلیکیشن (App Key) - باید با فایل .env لاراول (PUSHER_APP_KEY) یکی باشد
VITE_PUSHER_APP_KEY=dLqP31MIZy3LQm10QtHe9ciAt

# کلاستر (معمولا در حالت Self-hosted روی mt1 تنظیم می‌شود)
VITE_PUSHER_APP_CLUSTER=mt1

# آدرس هاست سوکت (بدون http/ws - فقط دامنه)
VITE_PUSHER_HOST=ws.eitebar.ir

# پورت سوکت (معمولاً 80 برای http و 443 برای https)
VITE_PUSHER_PORT=80

# اگر SSL دارید (https)، این را true و پورت را 443 کنید
VITE_PUSHER_FORCE_TLS=false